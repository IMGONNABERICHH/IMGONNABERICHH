# Archived capture — 7-langes.com

`ARCHIVE-7langes-original-2026-08-10.html` is the 7 L'ANGES studio site exactly
as https://7-langes.com served it at **23:32 UTC on 2026-08-10**, fetched
unmodified. 31,964 bytes, MD5 `d7062971e713015d65fc2f85ce35aa8c`.

Kept because the live site was later overwritten by a publish of the TWS
publication build, and this was the only copy of the studio page in existence
outside Netlify's deploy history.

## Read this before treating it as "the original"

This capture does **not** contain:

- an angel animation — the only animations are a `fadeIn` keyframe and the
  cursor-parallax masthead; the three "angel" string matches are all
  "Los Angeles"
- a Calendly embed — zero matches

Raven has referred to an earlier version with an angel animation. That version
predates this capture and was **not** what the domain served on 2026-08-10, so
it is not preserved here and does not exist anywhere in the build container.
Recovering it means finding the right entry in the Netlify deploy history for
site `reliable-torte-ed34ef` — each deploy has a permanent preview URL that can
be inspected without publishing.

Also note the contact form here still posts to `https://formspree.io/f/YOUR_FORM_ID`,
an unreplaced placeholder — inquiries submitted against this version go nowhere.
